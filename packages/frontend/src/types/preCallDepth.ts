/**
 * Pre-call generation depth (only when mode is Pre-Call Breakdown).
 */

export type PreCallDepth = "instant" | "deep";

/** Matches API field `precall_depth`. */
export type PrecallDepth = PreCallDepth;
